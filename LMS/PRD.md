# Gracified LMS — Product Requirements Document (PRD)

| | |
|---|---|
| **Product** | Gracified LMS |
| **Document** | PRD — Version 1.1 |
| **Status** | Living document — reflects current codebase state |
| **Owner** | Product Manager |
| **Surfaces** | Web app (React 18 / Vite / Tailwind), Mobile app (Expo / React Native), Backend API (Node.js / Express / MongoDB + Socket.io) |
| **Primary audience** | Schools, school administrators, teachers, independent tutors, and students |

> **Document basis.** This PRD was generated by studying the actual codebase (`backend/`, `frontend/`, `mobile/`), not the legacy `README.md` / `PRODUCT_GUIDE.md` / `product.md`, which are out of date. Where the code and the legacy docs disagree, this document reflects the code.

---

## 1. Executive summary

Gracified LMS is a multi-tenant learning management platform covering the complete teaching lifecycle: onboarding, school and classroom administration, curriculum delivery, live lessons, assessments, payments, marketing, analytics, and AI-assisted authoring — on web and mobile.

The platform serves two seller types — **institutional schools** (School Admins + Teachers) and **independent tutors** (Personal Teachers) — and one buyer type: **students**, who discover or receive class links, enroll, pay where required, learn from topics, join live sessions, submit work, and receive results.

The product is already substantially built. This PRD describes what exists today (verified against source), what remains to be built, and the requirements that govern both.

---

## 2. Problem statement

Teachers and schools juggle multiple disconnected tools to run a class: scheduling, content delivery, live calls, assessments, payments, and communication. This results in:

- **Fragmented workflows** — content, payments, and collaboration live in separate tools.
- **Lost revenue** — independent tutors lack a simple way to monetize classes (per-lecture, per-topic, weekly, monthly, one-time).
- **No audit trail** — schools cannot reliably track who paid, what was paid for, and enrollment status.
- **High authoring effort** — creating syllabi, topics, assignments, and exams is manual and time-consuming.
- **Poor reach** — small providers cannot afford marketing infrastructure to grow their student base.
- **External examiner/marker friction** — schools need to share answer scripts with external markers securely, without exposing full accounts.

### Opportunity

A single workspace that unifies classroom management, monetization, live delivery, assessment, external grading, and growth tools — with an AI assistant to reduce authoring effort — replaces the tool sprawl for both schools and independent tutors.

---

## 3. Goals and non-goals

### 3.1 Goals

| # | Goal | Status |
|---|---|---|
| G1 | Make it simple for a teacher to create and run a class from one workspace | Built (web + mobile) |
| G2 | Give students a reliable, accessible learning experience on web and mobile | Built |
| G3 | Support paid and free education products with auditable payment records | Built (Paystack + Stripe) |
| G4 | Provide strong role-based access across platform, school, classroom, and student data | Built (API + UI enforced) |
| G5 | Reduce authoring effort through AI-generated classrooms, topics, syllabi, assignments, exams, slides, marketing emails/campaigns, and Q&A answers | Built |
| G6 | Let schools securely share answer scripts with external markers/examiners | Built (script sharing) |
| G7 | Let teachers broadcast public lectures/seminars to guests, with live and recording access | Built |
| G8 | Keep web and mobile behavior consistent (auth, publishing, assessments, payments, collaboration) | Partial — gaps listed in §12 |

### 3.2 Non-goals (v1.x)

- Multi-language support.
- Full offline-first mobile experience (network-error + retry states exist).
- Content marketplace / institution-to-institution sharing.
- Self-hosted or on-prem deployments.
- Native iOS/Android outside Expo/React Native.
- Student self-serve refunds (admin/root-admin only today).

---

## 4. Success metrics

### 4.1 North-star metric

**Weekly active classrooms with at least one enrolled, active student** (proxy for a functioning teaching business on the platform).

### 4.2 Supporting metrics

| Category | Metric | Target |
|---|---|---|
| Activation | % of new teachers who publish a class within 7 days | ≥ 50% |
| Activation | % of new students who complete email verification | ≥ 80% |
| Engagement | Median weekly active days per active student | ≥ 3 |
| Monetization | % of paid classrooms with ≥ 1 completed payment | ≥ 40% |
| Monetization | Payment confirmation success rate | ≥ 98% |
| Assessment | % of published assignments/exams with ≥ 1 submission | ≥ 60% |
| Collaboration | % of active classrooms that launched ≥ 1 live session/month | ≥ 35% |
| Public classes | % of published public lectures/seminars with ≥ 1 guest attendee | ≥ 50% |
| Script sharing | % of share-config-enabled exams/assignments with ≥ 1 access session | ≥ 40% |
| AI | % of AI-generated drafts saved by teachers | ≥ 30% |
| Retention | Monthly cohort retention of teachers (M1) | ≥ 60% |
| Reliability | Payment verification failure rate | ≤ 0.5% |
| Reliability | API 5xx error rate | ≤ 0.1% |

---

## 5. Personas

| Persona | Role | Description | Primary jobs-to-be-done |
|---|---|---|---|
| P1 — Platform Operator | Root Admin | Owns and operates the platform | Manage users, plans, platform settings (fees, AI provider, subscription gate), payouts, feedback, global reports |
| P2 — School Principal | School Admin | Runs one or more schools | Onboard teachers (invite/bulk-invite), manage classrooms/students, school revenue, marketing |
| P3 — Teacher | Employee instructor | Delivers classes under a school | Manage assigned classrooms, topics, assessments, learner progress, live sessions |
| P4 — Personal Teacher | Independent tutor | Owns the teaching business | Everything a teacher does + payments, payouts, subscriptions, marketing, guest/public classes |
| P5 — Student | Learner | Enrolls and learns | Discover classes, pay, learn topics, track video progress, join live sessions, submit work, view reports |
| P6 — Guest attendee | Anonymous | Consumes a public lecture/seminar | Join live or watch recording, optionally after payment, without an account |

---

## 6. Roles & permissions matrix

Roles enum (backend `User.role`): `student`, `teacher`, `personal_teacher`, `school_admin`, `root_admin`. `root_admin` passes every `authorize()` gate.

| Capability | Root Admin | School Admin | Teacher | Personal Teacher | Student |
|---|---|---|---|---|---|
| Platform settings, plans, fees, AI provider | ✅ | ❌ | ❌ | ❌ | ❌ |
| Manage users / bulk-invite teachers | ✅ | ✅ (school) | ❌ | Own students | ❌ |
| Manage schools | ✅ | Own schools | ❌ | ❌ | ❌ |
| Create/manage classrooms & public classes | ✅ | ✅ (school) | ✅ (assigned) | ✅ (own) | ❌ |
| Manage topics (incl. video upload, activate/complete) | ✅ | ✅ | ✅ (assigned) | ✅ (own) | View (enrolled/paid) |
| Manage assignments/exams | ✅ | ✅ | ✅ (assigned) | ✅ (own) | Submit |
| Grade submissions (manual / AI theory marking) | ✅ | ✅ | ✅ | ✅ | ❌ |
| Script sharing (external marker links) | ✅ | ✅ | ✅ | ✅ | ❌ |
| Payments, refunds, disbursements | ✅ (refund/approve) | School revenue | ❌ | Own payouts | Pay/enroll |
| Marketing engine | ❌ | ✅ | ❌ | ✅ | ❌ |
| AI assistant (all 7 modes) | ✅ | ✅ | ✅ | ✅ | ❌ |
| Launch live sessions / whiteboard voice | ✅ | ✅ | ✅ | ✅ | Join |
| Reports & analytics | ✅ | ✅ | ✅ | ✅ | Own report |
| Feedback | Review | — | Review | Review | Submit |
| Public class guest admission | ❌ (manage) | ✅ | ✅ | ✅ | Guest join |

> Authorization is enforced in the backend routes (`auth` → `authorize(...)` → resource checks such as `hasSchoolAccess`, `canAccessExam`, ownership). UI hiding is never the security boundary.

---

## 7. Current feature inventory (verified against source)

### 7.1 Authentication & onboarding
- Register paths: Student, School Admin, Personal Teacher. Teachers under a school are **invited** (tokenized link → `/set-password`).
- Email verification via token/OTP; unverified users blocked by `auth` middleware (root_admin exempt) and redirected on both web and mobile.
- Forgot password (request → OTP/token → reset). Change password (requires current password).
- 2FA: `POST /api/auth/generate-otp` (6-digit OTP, 5-min expiry + 10 backup codes), `POST /api/auth/verify-otp`; `twoFAEnabled` defaults **on** for root_admin; `tempLoginToken` supports staged login.
- JWT sessions; mobile persists token in AsyncStorage and hydrates via `GET /api/auth/me`.
- Profile/logo upload (`POST /api/auth/upload-logo`, Cloudinary w/ local fallback, 2 MB).
- Subscription gate: `subscriptionCheck`/`enforceSubscription` middleware; `pay_as_you_go` exempt; global kill-switch `Settings.subscriptionCheckingEnabled`.

### 7.2 Schools & multi-school administration
- School CRUD (`name`, `logoUrl`, `shortCode`, SEO `slug`); root/school admins; School Admin can belong to **multiple schools** (school switcher on web).
- Teacher invites: single (`POST /api/users`) and **CSV bulk invite** (`POST /api/users/bulk-invite`, 5 MB).
- Public school page `/s/:identifier`.

### 7.3 Classrooms
- Classroom fields: name, description, learningOutcomes, subject, level (Pre-Primary → Postgraduate → Professional → Vocational → Other), teacherId, schoolId[], students[], schedule[] (dayOfWeek + start/end), capacity (default 30), pricing {type: per_lecture/per_topic/weekly/monthly/one_time/free, amount}, isPaid, classFormat, publicAccess, published, isPrivate, currentTopicId, introVideo, whiteboardUrl/whiteboardActiveAt, shortCode + slug.
- Publication lifecycle: `POST /api/classrooms/:id/publish` and `/unpublish` (idempotent; sends `class_published` notifications).
- Enrollment: `POST /api/classrooms/:id/enroll` (self) with subscription rules applied.
- Share links: `/c/:shortCode`, slug-based `/c/:slug`.

### 7.4 Public lectures & seminars (classFormat: public_lecture / public_seminar)
- `publicAccess` config: `allowGuestAccess` (guest access always enabled for public formats), duration window (`startsAt`/`endsAt`, duration days/weeks), `recordingUrl` (YouTube/Vimeo), `joinInstructions`.
- Public catalog: `GET /api/classrooms/catalog` (public) with search + filters (format, subject, price, free/paid).
- Public classroom page: share modal (QR, WhatsApp/Facebook/X), **Guest Join** (name + email → live join or recording watch; Paystack checkout when paid) → records **PublicAttendee** (`status`: pending_payment/admitted/watched_recording; `accessType`: live/recording).
- Web UI badges: "Live Now" (polls catalog), "Recording", "Window Closed".
- PublicClasses management page (staff) with "Decommission Public Class?" purge confirmation.

### 7.5 Topics & curriculum progression
- Topic fields: name, description, lessonsOutline, order, materials[] (video/document/link/text), recordedVideos[] (file or URL, label, order), isPaid/price, duration {mode: day/week/month/year/not_sure, value}, status (pending/active/completed), startedAt/completedAt/expectedEndDate, nextTopicId, completedBy.
- Progression engine: `POST /api/topics/:id/activate`, `/complete`, `PUT /:id/set-next`, `POST /:id/reset`, `PUT /topics/reorder`, `POST /topics/bulk-create/:classroomId`; daily midnight cron `checkAutoProgression()` advances overdue active topics.
- **Video management:** `POST /:id/upload-video` (Cloudinary/local, 500 MB), `POST /:id/add-video-url`, `DELETE /:id/videos/:videoId`, label + reorder endpoints.
- **Student progress tracking:** `TopicProgress` model — `GET/POST /api/topics/:id/progress` records watchedVideoIds, completionPercentage, lastActiveVideoId (unique per user+topic).
- Topic management timeline UI `/classrooms/:id/manage-topics`.

### 7.6 Assignments
- Types: **MCQ** (correctOption stored, `publishResultsAt` controls release) and **theory** (per-question `markingPreference: 'ai' | 'manual'`).
- CRUD + publish/unpublish, classroom/topic link, dueDate, maxScore.
- Submissions embedded in Assignment: answers (text or selected options), files[], questionScores[], score, feedback, status (`submitted` → `graded` → `returned`).
- Grading: `PUT /api/assignments/:id/grade` (MCQ), `PUT /:id/grade-theory` (manual or AI), `GET /:id/results`, `POST /:id/notify` (reminder).
- Notifications on create, submission (`new_submission`), and grading (`assignment_graded`) via email + push + in-app.

### 7.7 Exams
- Exam fields: title, description, creatorId, schoolId, classId, duration (minutes), accessMode (`registered` | `open`), startTime/endTime/dueDate, questions[] (mcq/theory, options, correctOption, maxScore), isPublished, resultsPublished, resultPublishTime, linkToken (8-char).
- **Open access:** guests identified by candidateName/candidateEmail in `ExamSubmission` (studentId nullable).
- Exam-taking: `ExamSubmission` with status (`in-progress` → `submitted` → `graded`), timeRemaining, auto-submit, per-answer scores, totalScore.
- Results: `POST /api/exams/:id/publish-results` → scheduler `processPendingExamResults()` emails result links when public.
- Exam creator UI with AI question generation and LaTeX (KaTeX) rendering.

### 7.8 AI assistant (7 modes)
Providers: **Groq** (default, LLaMA 3.3 70B) or **Gemini**; selected via `Settings.activeAIProvider`; endpoints:
- `POST /api/ai/generate-classroom` → name/description/learningOutcomes
- `POST /api/ai/generate-topic` → topic with duration
- `POST /api/ai/generate-syllabus` → 5–10 ordered topics ("Import Syllabus")
- `POST /api/ai/generate-assignment` → MCQ/theory questions (count ≤ 20)
- `POST /api/ai/generate-exam` → timed exam (count ≤ 30, LaTeX-aware)
- `POST /api/ai/generate-powerpoint` + `POST /api/ai/download-powerpoint` → real `.pptx` with school/tutorial logo, speaker notes, themed slides
- `POST /api/ai/qna-assistant` → academic answer + follow-up suggestions
- `POST /api/ai/generate-marketing-email` + `generate-marketing-campaign` → drip email content
- All responses forced to JSON; errors logged without exposing secrets; students never see the panel.

### 7.9 Live lessons & collaboration
- **Zoom**: `POST /api/zoom/create-meeting/:classroomId` (JWT-based Zoom API), `GET /api/zoom/meeting/:classroomId`; CallSession records classroom/startedBy/link/startedAt/isPaid/amount.
- **Google Meet**: OAuth consent (`/api/google-auth/*`) storing `googleOAuthRefreshToken` for calendar/meet scopes; `GoogleMeetAuth` component.
- **Attendance**: `Attendance` per student per CallSession (unique index prevents duplicates).
- **Whiteboard**: Socket.io rooms keyed by classId; persisted strokes in `Whiteboard` model; virtual 10000×40000 canvas; teacher tools (width, undo, clear, night/dark mode, export PNG, **lock board**, selective user allow/unlock); student controls (follow teacher, raise hand); presence cursors/colors; real-time `wb:*` events.
- **WebRTC voice + video**: teacher-initiated voice (`wb:voice-start`), SDP offer/answer + ICE signaling, mute/unmute, **force-mute / mic-lock** (teacher), per-user mute/video state, voice-enabled gating.

### 7.10 Q&A board
- Boards tied to classroom (+optional topic), `shareableLink` token, `isPublic`, `allowAnonymous`, `hideQuestions`, `isActive`.
- Endpoints: create/list boards, post questions (students), answer, accept answer.
- Web: Q&A center `/qna/:token`, presentation mode `/qna/:token/present`. Mobile: Q&A center (WebView).

### 7.11 Payments, billing & disbursements
- **Stripe**: `POST /api/payments/checkout/:classroomId` (Checkout Session), signature-verified webhook, `POST /api/payments/:id/refund` (root admin).
- **Paystack**: initiate → inline widget → verify; webhook; guest checkout for public classes.
- Payment record: userId, type (`class_enrollment`, `topic_access`, `lecture_access`, `subscription`, `personal_teacher_subscription`), classroomId/topicId/callSessionId/planId, amount, currency, status, paystackReference/stripePaymentId, and financial breakdown: `taxAmount`, `vatAmount`, `serviceFeeAmount`, `payoutAmount` (computed from `Settings.taxRate/vatRate/serviceFeeRate`).
- **Disbursements**: root admin approves `POST /api/disbursements/approve/:paymentId` (generates `payoutReference`, sends `payout_received` notification + detailed email with fee breakdown); pending/history/my-payouts views; user bank details + payout frequency (daily/weekly/monthly) in Profile.
- **Subscriptions**: plans (trial/pay_as_you_go/weekly/monthly/yearly); `UserSubscription` (one per user), status lifecycle, `POST /api/user-subscriptions/issue-free-access` (root admin), trial = 14 days.

### 7.12 Notifications (3 channels)
- **In-app**: Notification model (userId, message, type, entityId/entityRef, read). Endpoints: list (latest 20), mark-read, mark-all-read, unread-count.
- **Email**: Brevo transactional (`utils/email.js`) + Nodemailer; templated wrapper.
- **Push**: Expo push via `User.expoPushToken`; Notification post-save hook triggers push.
- Notification types: `class_reminder`, `assignment_reminder`, `assignment_result`, `payment_success`, `payment_received`, `payout_received`, `new_assignment`, `new_submission`, `assignment_graded`, `subscription_success`, `student_enrolled`, `topic_activated`, `new_class_created`, `class_published`, `classroom_ended`, `marketing_job`.

### 7.13 Script sharing (external markers)
- `ScriptShareConfig` per exam/assignment: `isShareable`, `defaultAccessType` (view/grade), `otpLifespanMinutes`, `accessDurationMinutes`, `assignedMarkerId`.
- `ScriptAccessSession`: shareToken, submissionRef(s), groupName, requester info, **hashed OTP**, accessToken (time-limited), status (`pending_otp` → `active` → `saved`/`expired`), gradingSnapshot, savedSubmissions.
- Flow: teacher enables sharing → generates single or **group link** → external marker requests access (name/email) → authorities (creator, class teacher, school admins, assigned marker, root admins) receive OTP email → marker enters OTP → time-limited `view` or `grade` session → can save grading progress (`PATCH grade`) and **finalize** (`POST finalize`), which writes grades back to the real submission.
- Public page `/shared-script/:shareToken` on web.

### 7.14 Marketing engine
- Entities: MarketingList, MarketingContact (auto `unsubscribeToken`, timezone default Africa/Lagos), MarketingTemplate, MarketingCampaign (types: campaign_step/welcome/birthday/anniversary/referral/reactivation), MarketingJob, MarketingHoliday (month/day blackout), MarketingSendLog (dedupe indexes).
- CSV contact import (`POST /api/marketing/contacts/import-csv`, 10 MB), lists + add-contacts, templates + test send, campaigns create/start/pause, one-click unsubscribe.
- Scheduler processes up to 40 campaign-step emails/tick and 20 greeting recipients/tick; send logs + job analytics.
- AI can generate single emails and multi-step drip campaigns.

### 7.15 Reports & analytics
- Role-scoped reports: `GET /api/reports/student/me`, `/class/:classId`, `/all-students?schoolId=`, `/school/:schoolId`, `/admin/overview`.
- Web: per-role dashboards with student performance tables, grade distribution, enrollment, revenue; **CSV + PDF export** (jsPDF + autotable); school-switcher aware.
- Mobile: role-scoped Reports screen with PDF export.

### 7.16 Feedback
- `FeedbackRequest`: type (classroom/platform), status (pending/completed/dismissed), rating 1–5, comment; teacher/admin review surface `/feedbacks`.

### 7.17 Platform settings (root admin)
- taxRate, vatRate, serviceFeeRate, subjects[], `subscriptionCheckingEnabled`, `activeAIProvider`; managed at `/platform-settings`; subscription plans at `/subscription-plans-admin`.

### 7.18 Onboarding & platform UX
- Role-based OnboardingTour (spotlight masks) for first logins.
- Dark/light theme (web + mobile), school switcher, splash screen, error boundaries, loading/empty/error + retry states.

---

## 8. Mobile application (current state)

### 8.1 What's built
- Navigation: auth stack → verify-email gate → MainTabs (Dashboard, Classes, Payments, Assignments [students only], Profile) + detail stacks.
- 26 screens: AuthIntro, Login, Register (student role), VerifyEmail, ForgotPassword, NetworkTest, Dashboard, Classrooms, ClassroomDetail, TopicDetail, VideoPlayer, Assignments, AssignmentDetail, Exams, ExamCenter, ExamDetail, Payments, PaystackWebView, QnACenter (WebView), Whiteboard (WebView), Notifications, SharedResource (deep links), ManageTeachers, AIAssistant, Reports, Profile.
- Session persistence via AsyncStorage + `GET /auth/me`; push-token registration; deep links (custom scheme + HTTPS hosts); Paystack via WebView; AI panel with PPTX download/share; PDF report export; avatar/logo upload; bank payout form (school_admin/personal_teacher); dark/light auto theme.

### 8.2 Known gaps (mobile vs web)
| # | Gap | Impact |
|---|---|---|
| M1 | No manual assignment/exam/class authoring UI (AI-assisted creation only) | Staff cannot build assessments by hand on mobile |
| M2 | No subscription/billing upsell UI (fields returned by API unused) | Teachers cannot manage plans on mobile |
| M3 | No 2FA enrollment/verify flow in login | 2FA users effectively locked out of mobile |
| M4 | No root-admin console (users/schools/settings/payouts oversight) | Platform operator can't administer from mobile |
| M5 | No marketing/landing experience; AuthIntro is a light carousel | Poor discovery experience |
| M6 | No class roster management, per-class student invites, schedule/calendar | Teacher gap |
| M7 | No student progress graph beyond Reports; no gradebook, certificates, parent view | Student gap |
| M8 | Role gating is partially client-side; ownership checks inconsistent in some screens | Risk of unauthorized UI access (backend still guards data) |

---

## 9. User stories (prioritized)

Stories follow the "As a… I want… so that…" format, with acceptance criteria. Priorities: **M**ust, **S**hould, **C**ould, **W**on't-have-now. The current build is the baseline; stories marked **(planned)** are remaining/new work.

### 9.1 Authentication & onboarding
| ID | Priority | Story | Acceptance criteria |
|---|---|---|---|
| AUTH-1 | M | As a prospective user, I want to register as a Student, School Admin, or Personal Teacher — and as a School Admin, invite teachers via tokenized links — so that everyone joins through the correct identity. | Three registration paths work; invited teachers complete setup at `/set-password`; no access before email verification. |
| AUTH-2 | M | As a user, I want my session to persist across web and mobile, so that I don't have to log in repeatedly. | JWT sessions; mobile hydrates via `GET /auth/me`; unverified users gated to verification. |
| AUTH-3 | M | As a user who forgot my password, I want to request an OTP, reset it, and change my password, so that I can regain and control access securely. | Forgot → OTP → reset works; change-password requires current password; OTPs expire. |
| AUTH-4 | M | As a user, I want optional 2FA with backup codes (mandatory for Root Admins), so that my account is protected even if my password leaks. | OTP generate/verify endpoints; 10 backup codes; `twoFAEnabled` default on for root_admin. |
| AUTH-5 | M | As a system, I want auth endpoints rate-limited, so that brute-force attempts are throttled. | `/api/auth` + `/api/google-auth` limited to 100 req / 15 min / IP. |
| AUTH-6 | M | As a School Admin or Personal Teacher, I want a subscription gate, so that I must be on a valid plan (or pay-as-you-go) to teach — with a global kill-switch for admins. | Gate enforces plan status; `Settings.subscriptionCheckingEnabled` toggles globally; pay-as-you-go exempt. |
| AUTH-7 | S | **(planned)** As a mobile user with 2FA, I want to enroll and verify 2FA in the app, so that I'm not locked out of mobile. | Closes gap M3; OTP + backup code flow in mobile login. |
| AUTH-8 | S | **(planned)** As a mobile seller, I want to view and manage my subscription plan in the app, so that I can upgrade without a desktop. | Closes gap M2; plan status, purchase, and activation from mobile. |

### 9.2 Schools & users
| ID | Priority | Story | Acceptance criteria |
|---|---|---|---|
| SCH-1 | M | As a School Admin or Root Admin, I want to create, brand, and manage one or more schools from one account, so that I can run institutions efficiently. | School CRUD; multi-school membership + switcher; public school page `/s/:identifier`. |
| SCH-2 | M | As a School Admin, I want to invite teachers individually or via CSV bulk-invite, so that onboarding staff is fast. | Invite + `POST /users/bulk-invite`; tokenized set-password flow. |
| SCH-3 | M | As an admin, I want school-scoped user management, so that I can manage only my school's users. | `/users` scoped by role/school; status changes. |
| SCH-4 | S | **(planned)** As a Root Admin, I want a mobile admin console, so that I can manage the platform from my phone. | Closes gap M4; users/schools/settings/payouts oversight in mobile. |

### 9.3 Classrooms & public classes
| ID | Priority | Story | Acceptance criteria |
|---|---|---|---|
| CLS-1 | M | As a teacher, I want to create classrooms with full configuration (level, schedule, capacity, pricing, format, visibility) and publish/unpublish them, so that I can launch and control my classes. | Classroom CRUD; idempotent publish/unpublish with `class_published` notifications. |
| CLS-2 | M | As a student, I want to enroll only in classes I'm allowed to join, so that visibility, payment, capacity, and subscription rules are respected. | Enrollment gated server-side; capacity respected. |
| CLS-3 | M | As a teacher, I want to run public lectures and seminars with guest access (live or recording) and optional payment, so that I can reach students beyond my enrolled base. | Guest join/recording; public access window; paid guest checkout; `PublicAttendee` recorded. |
| CLS-4 | M | As a prospective student, I want to browse a public catalog with filters and share class links (shortCode/slug, QR, social), so that I can discover and share classes easily. | Catalog filters (format/subject/price); `/c/:shortCode` + slug links; QR/social share. |
| CLS-5 | M | As a visitor, I want to see Live Now / Recording / Window Closed status on public class cards, so that I know when to join. | Live-status detection via catalog polling. |
| CLS-6 | S | As an admin, I want to decommission a public class with a purge confirmation, so that removals are deliberate and recoverable. | Decommission dialog with PURGE confirmation. |
| CLS-7 | S | **(planned)** As a mobile teacher, I want to manage my class roster and invite students, so that I can run classes from my phone. | Closes gap M6; roster + invites in mobile. |

### 9.4 Topics & progression
| ID | Priority | Story | Acceptance criteria |
|---|---|---|---|
| TOP-1 | M | As a teacher, I want to create, reorder, and bulk-create topics with materials and recorded videos, so that my curriculum is structured and rich. | Topic CRUD/reorder/bulk-create; video upload/URL/label/reorder/delete. |
| TOP-2 | M | As a teacher, I want a progression engine that activates and auto-completes topics in order, so that the curriculum flows without manual juggling. | activate/complete/auto-next (order or `nextTopicId`); manual set-next/reset. |
| TOP-3 | M | As a teacher, I want overdue topics to auto-progress, so that the schedule stays on track even when I forget. | Daily midnight cron; idempotent and retry-safe. |
| TOP-4 | M | As a teacher, I want per-topic pricing overrides, so that I can sell topics individually. | Paid access checks enforced before topic access. |
| TOP-5 | M | As a student, I want my video watch progress tracked, so that I can resume where I left off. | `TopicProgress` records watched videos, %, last watched; unique per user+topic. |
| TOP-6 | S | **(planned)** As a mobile teacher, I want to author topics and upload videos in the app, so that I can prepare lessons on the go. | Closes gap M1. |

### 9.5 Assignments & exams
| ID | Priority | Story | Acceptance criteria |
|---|---|---|---|
| ASG-1 | M | As a teacher, I want to create and publish MCQ and theory assignments, so that students receive assessable work. | CRUD + publish; MCQ `correctOption` + `publishResultsAt`; theory `markingPreference` ai/manual. |
| ASG-2 | M | As a student, I want to submit answers with optional files; as a teacher, I want to grade with per-question scores, feedback, and AI assistance, so that marking is fast and fair. | Submit/grade/grade-theory flows; results released per config. |
| ASG-3 | M | As a system, I want to hide correct answers until submission or result release, so that assessments stay fair. | Correct answers stripped from student responses server-side. |
| ASG-4 | M | As a teacher, I want timed exams with registered or open access and controlled availability, so that I can run formal assessments. | Exam CRUD; start/end/due windows; accessMode registered/open. |
| ASG-5 | M | As a student, I want exam attempts to persist with a countdown and auto-submit, so that I never lose my work. | `ExamSubmission` in-progress persistence, `timeRemaining`, auto-submit, result emails via scheduler. |
| ASG-6 | M | As a teacher or student, I want math to render correctly, so that science and math questions display properly. | LaTeX/KaTeX rendering in exam and exam creator. |
| ASG-7 | S | **(planned)** As a mobile teacher, I want to build assignments and exams manually in the app, so that I can create assessments on the go. | Closes gap M1. |
| ASG-8 | C | **(planned)** As a mobile student, I want offline submit and result push, so that I can complete work without a stable connection. | Closes part of gap M7. |

### 9.6 AI assistant
| ID | Priority | Story | Acceptance criteria |
|---|---|---|---|
| AI-1 | M | As a staff user, I want seven AI authoring modes (class, topic, syllabus, assignment, exam, slides, Q&A) on web and mobile, so that I can create content faster. | All modes present and reachable in AI panel on web + mobile. |
| AI-2 | M | As a teacher, I want to apply or download AI output, so that drafts quickly become real content. | Apply actions for class/topic/syllabus/assignment/exam; slides copy + `.pptx` download. |
| AI-3 | M | As a Root Admin, I want to choose the AI provider (Groq or Gemini), so that I control cost and quality. | `Settings.activeAIProvider`; JSON-only responses; output treated as draft until saved. |
| AI-4 | M | As a School or Personal Teacher, I want AI to draft marketing emails and drip campaigns, so that I can grow my student base. | `generate-marketing-email` + `generate-marketing-campaign` endpoints. |
| AI-5 | S | **(planned)** As a PM/operations owner, I want AI usage and quality analytics, so that I can tune the feature. | Usage/rate-limit observability added. |

### 9.7 Live & collaboration
| ID | Priority | Story | Acceptance criteria |
|---|---|---|---|
| LIVE-1 | M | As a teacher, I want to launch Zoom or Google Meet sessions with attendance tracking, so that live teaching is straightforward. | Zoom/Meet launch; `CallSession` + `Attendance` recorded (unique per session). |
| LIVE-2 | M | As a teacher or student, I want a full collaborative whiteboard (draw, lock, follow, undo, dark mode, export), so that lessons are interactive. | Socket toolkit (`wb:*`) + persisted strokes; teacher lock/allow controls. |
| LIVE-3 | M | As a teacher, I want voice and video with mute and lock controls, so that I can manage a live class. | WebRTC voice/video; teacher-initiated voice; force-mute; mic-lock; raise hand. |
| LIVE-4 | M | As a teacher, I want per-lecture payments, so that I can charge for individual sessions. | `CallSession.isPaid`/`amount` enforced before joining. |
| LIVE-5 | M | As a mobile user, I want deep-link returns after Q&A or whiteboard use, so that I land back in the app. | WebView return paths restore the correct screen. |

### 9.8 Q&A
| ID | Priority | Story | Acceptance criteria |
|---|---|---|---|
| QNA-1 | M | As a teacher, I want Q&A boards with share tokens, public/anonymous/hide-questions modes, moderation, and a server-validated presentation view, so that classes engage live. | Boards CRUD; question posting/answering; presentation access validated server-side. |

### 9.9 Payments, subscriptions, disbursements
| ID | Priority | Story | Acceptance criteria |
|---|---|---|---|
| PAY-1 | M | As a student, I want to pay via Paystack or Stripe with server-side verification, so that I'm enrolled only after a valid payment. | Checkout/webhook/verify flows; idempotent confirmation; enrollment after verification. |
| PAY-2 | M | As a seller, I want fee breakdowns (tax/VAT/service fee) and payout amounts on every payment, so that my finances are transparent. | Breakdown stored on each Payment record. |
| PAY-3 | M | As a student or seller, I want searchable payment history on web and mobile, so that I can reconcile transactions. | `GET /payments/my-payments`; search by reference; payer/purpose/status shown. |
| PAY-4 | M | As a seller, I want subscription plans with one active subscription and free-access issuance, so that my plan status is always clear. | Plan lifecycle; `UserSubscription` (one active per user); `issue-free-access`. |
| PAY-5 | M | As a Root Admin, I want to approve disbursements with references and detailed payout emails, so that payouts are auditable. | pending/approve/history/my-payouts; `payoutReference`; payout emails with fee breakdown. |
| PAY-6 | M | As a Root Admin, I want to refund payments, so that disputes can be resolved. | `POST /payments/:id/refund`. |
| PAY-7 | S | **(roadmap)** As a seller or student, I want coupons, invoices, and self-serve refund requests, so that billing is flexible. | Coupons/invoices/refund requests delivered. |

### 9.10 Notifications
| ID | Priority | Story | Acceptance criteria |
|---|---|---|---|
| NOT-1 | M | As a user, I want an in-app feed with read/unread state and entity links, so that I don't miss important events. | Feed list, mark-read, mark-all-read, unread count. |
| NOT-2 | M | As a user, I want email notifications for key events, so that I stay informed even off-platform. | Brevo/SMTP sends for the full event list in §7.12. |
| NOT-3 | M | As a mobile user, I want push notifications, so that I'm notified even when the app is closed. | Expo push via `expoPushToken`. |
| NOT-4 | M | As a system, I want scheduled reminders, so that classes, exams, and marketing run on time. | 15-minute class reminders; exam-result emails; marketing queue; topic auto-progression. |

### 9.11 Script sharing
| ID | Priority | Story | Acceptance criteria |
|---|---|---|---|
| SCR-1 | M | As a teacher, I want to configure script sharing per exam or assignment, so that external markers can be invited safely. | Config: enable/disable, access type (view/grade), OTP lifespan, session length, assigned marker. |
| SCR-2 | M | As a teacher, I want single and group share links with an OTP approval flow, so that markers get access without accounts. | Link generation (single + group); request-access notifies authorities with OTP email. |
| SCR-3 | M | As an external marker, I want a time-limited view or grade session where I can save progress and finalize, so that grades sync back to the real submission. | `PATCH /grade` snapshots; `POST /finalize` writes grades back. |
| SCR-4 | M | As a system, I want OTPs hashed and sessions auto-expired, so that sharing stays secure. | OTP hashed before storage; access tokens TTL; sessions auto-expire. |
| SCR-5 | S | **(planned)** As a mobile teacher, I want script sharing in the app, so that I can invite markers on the go. | Closes gap M5 partially; share-link generation + status on mobile. |

### 9.12 Marketing
| ID | Priority | Story | Acceptance criteria |
|---|---|---|---|
| MKT-1 | M | As a School or Personal Teacher, I want contacts, lists, templates, campaigns, holidays, and logs, so that I can run email marketing. | Full marketing CRUD + CSV contact import (10 MB). |
| MKT-2 | M | As a contact, I want one-click unsubscribe and protection from repeat sends, so that I'm not spammed. | Unsubscribe link per contact; dedupe indexes on send logs. |
| MKT-3 | M | As a teacher, I want AI-drafted emails and campaigns, so that content creation is fast. | AI marketing endpoints produce reviewable drafts. |
| MKT-4 | S | **(planned)** As a mobile seller, I want to manage marketing in the app, so that I can run campaigns from my phone. | Closes gap M5 partially. |

### 9.13 Reports & feedback
| ID | Priority | Story | Acceptance criteria |
|---|---|---|---|
| RPT-1 | M | As an admin, teacher, or student, I want role-scoped reports with CSV/PDF export, so that performance and revenue are trackable. | Per-role report endpoints; export on web (CSV/PDF) and mobile (PDF). |
| RPT-2 | M | As a teacher or admin, I want feedback requests with ratings and comments, so that I can improve classes. | Feedback lifecycle (pending/completed/dismissed); review surface. |
| RPT-3 | C | **(roadmap)** As a teacher, I want richer dashboards, attendance reporting, and calendar sync, so that I can plan better. | Delivered as roadmap items.

---

## 10. Verified API surface (reference)

Backend is mounted under `/api`; JWT bearer auth; `auth` → `authorize(roles)` → resource checks.

| Area | Endpoints |
|---|---|
| Auth | `POST /auth/register`, `POST /auth/login`, `POST /auth/verify-otp`, `POST /auth/generate-otp`, `POST /auth/resend-verification`, `POST /auth/forgot-password`, `POST /auth/reset-password`, `POST /auth/change-password`, `POST /auth/logout`, `GET /auth/me`, `PUT /auth/me`, `POST /auth/upload-logo` |
| Users | `GET /users` (roles: root/school admin + personal teacher), `POST /users`, `POST /users/bulk-invite`, `POST /users/expo-token`, `GET /users/my-students`, `GET/PUT/DELETE /users/:id` |
| Schools | `POST /schools`, `GET /schools`, `GET /schools/public/:identifier`, `GET /schools/:id`, `PUT /schools/:id`, `DELETE /schools/:id` |
| Classrooms | `GET /classrooms`, `GET /classrooms/catalog` (public), `GET /classrooms/active-meetings`, `GET /classrooms/public/:identifier`, `GET /classrooms/slug/:slug` (public), `POST /classrooms`, `GET /classrooms/:id`, `PUT /classrooms/:id`, `DELETE /classrooms/:id`, `POST /classrooms/:id/enroll`, `POST /classrooms/:id/publish`, `POST /classrooms/:id/unpublish` |
| Topics | `GET /topics/classroom/:classroomId`, `GET /topics/classroom/:classroomId/current`, `GET /topics/user/all`, `PUT /topics/reorder`, `POST /topics/bulk-create/:classroomId`, `POST /topics`, `GET /topics/:id`, `PUT /topics/:id`, `DELETE /topics/:id`, `POST /topics/:id/activate`, `POST /topics/:id/complete`, `PUT /topics/:id/set-next`, `POST /topics/:id/reset`, `POST /topics/:id/upload-video`, `POST /topics/:id/add-video-url`, `DELETE /topics/:id/videos/:videoId`, `PUT /topics/:id/videos/:videoId/label`, `PUT /topics/:id/videos/reorder`, `GET/POST /topics/:id/progress` |
| Assignments | `GET /assignments`, `GET /assignments/classroom/:classroomId`, `GET /assignments/:id`, `POST /assignments`, `PUT /assignments/:id`, `DELETE /assignments/:id`, `PUT /assignments/:id/publish`, `POST /assignments/:id/submit`, `GET /assignments/:id/submissions`, `PUT /assignments/:id/grade`, `PUT /assignments/:id/grade-theory`, `GET /assignments/:id/results`, `POST /assignments/:id/notify` |
| Exams | `GET /exams`, `GET /exams/classroom/:classroomId`, `GET /exams/:id`, `POST /exams`, `PUT /exams/:id`, `DELETE /exams/:id`, `POST /exams/:id/submit`, `POST /exams/:id/grade`, `POST /exams/:id/publish-results`, `GET /exams/:id/results`, `GET /exams/public/:token` |
| Payments | `POST /payments/checkout/:classroomId` (Stripe), `POST /payments/webhook`, `POST /payments/paystack/initiate`, `GET /payments/paystack/verify`, `POST /payments/paystack/webhook`, `GET /payments/my-payments`, `GET /payments/:id`, `POST /payments/:id/refund`, `POST /payments/payouts/approve/:paymentId` |
| Disbursements | `GET /disbursements/pending`, `GET /disbursements/history`, `POST /disbursements/approve/:paymentId`, `GET /disbursements/my-payouts` |
| Subscriptions | `GET /subscription-plans`, `GET /subscription-plans/:id`, `POST/PUT/DELETE /subscription-plans(/...)` (root admin); `GET /user-subscriptions/me`, `GET /user-subscriptions`, `POST /user-subscriptions`, `PATCH /user-subscriptions/:id/status`, `POST /user-subscriptions/issue-free-access` |
| Marketing | `GET/POST /marketing/contacts`, `PUT/DELETE /marketing/contacts/:id`, `POST /marketing/contacts/import-csv`, `GET/POST /marketing/lists`, `PUT /marketing/lists/:id`, `POST /marketing/lists/:id/add-contacts`, `DELETE /marketing/lists/:id`, `GET/POST/PUT/DELETE /marketing/templates(/...)`, `POST /marketing/templates/:id/send-test`, `POST /marketing/send`, `POST /marketing/send-async`, `GET /marketing/jobs/:id`, `GET/POST /marketing/campaigns`, `PUT /marketing/campaigns/:id`, `POST /marketing/campaigns/:id/start`, `POST /marketing/campaigns/:id/pause`, `GET/POST/PUT/DELETE /marketing/holidays(/...)`, `GET /marketing/logs`, `GET /marketing/unsubscribe/:token` (public) |
| Scripts | `GET/PUT /scripts/exam/:examId/share-config`, `GET/PUT /scripts/assignment/:assignmentId/share-config`, `POST /scripts/exam-submission/:submissionId/generate-link`, `POST /scripts/exam-submission/group-generate-link`, `POST /scripts/assignment-submission/:assignmentId/:studentId/generate-link`, `POST /scripts/assignment-submission/group-generate-link`, `GET /scripts/share/:shareToken` (public), `POST /scripts/share/:shareToken/request-access` (public), `POST /scripts/share/:shareToken/verify-otp` (public), `GET /scripts/session/:accessToken`, `PATCH /scripts/session/:accessToken/grade`, `POST /scripts/session/:accessToken/finalize` |
| AI | `GET /ai/provider`, `POST /ai/generate-classroom`, `/ai/generate-topic`, `/ai/generate-syllabus`, `/ai/generate-assignment`, `/ai/generate-exam`, `/ai/generate-powerpoint`, `/ai/download-powerpoint`, `/ai/qna-assistant`, `/ai/generate-marketing-email`, `/ai/generate-marketing-campaign` |
| Live | `POST /zoom/create-meeting/:classroomId`, `GET /zoom/meeting/:classroomId`, `GET /whiteboard/:classroomId`, `POST /whiteboard/:classroomId/publish`, `GET /google-auth/start-consent`, `GET /google-auth/oauth-callback`, `GET /google-auth/url`, `POST /google-auth/save-token` |
| Q&A | `POST /qna/boards`, `GET /qna/boards/classroom/:classroomId`, `GET /qna/boards/:id`, `POST /qna/boards/:id/questions`, `POST /qna/questions/:id/answers`, `PUT /qna/questions/:id` |
| Notifications | `GET /notifications/inapp`, `PUT /notifications/inapp/:id/read`, `PUT /notifications/inapp/mark-all-read`; internal webhook endpoints under `internalAuth` |
| Reports | `GET /reports/student/me`, `GET /reports/class/:classId`, `GET /reports/all-students`, `GET /reports/school/:schoolId`, `GET /reports/admin/overview` |
| Settings | `GET /settings` (public), `PUT /settings` (root admin) |
| Infra | `GET /api/health`, `/api-docs` (Swagger), socket events `wb:join/draw/draw-bulk/remove-stroke/clear/lock/theme/allow-user/voice-*/sdp-*/ice-candidate/mute-status/video-status/raise-hand/force-mute/cursor/viewport/follow` |

---

## 11. Data model (primary entities)

`User`, `School`, `Tutorial`, `Classroom`, `Topic`, `TopicProgress`, `Assignment` (embedded submissions), `Exam`, `ExamSubmission`, `Payment`, `CallSession`, `Attendance`, `PublicAttendee`, `QnABoard`, `QnAQuestion`, `Whiteboard` (strokes), `Notification`, `ScriptShareConfig`, `ScriptAccessSession`, `SubscriptionPlan`, `UserSubscription`, `FeedbackRequest`, `Settings`, marketing models (`MarketingList`, `MarketingContact`, `MarketingTemplate`, `MarketingCampaign`, `MarketingJob`, `MarketingHoliday`, `MarketingSendLog`).

Key indexes/constraints: unique email; unique school/classroom shortCode + slug; unique attendance (studentId, callSessionId); unique TopicProgress (userId, topicId); unique ScriptShareConfig (parentId, parentType); TTL on accessTokenExpiresAt; unique linkToken (exam); unique shareToken (script session); dedupe indexes on marketing send logs.

---

## 12. Non-functional requirements

### 12.1 Performance
- Paginate large classroom, user, payment, submission, notification lists; index classroom/school/user IDs, publication flags, payment references, timestamps.
- Mobile loads parallel classroom resources without blocking UI; API p95 < 500 ms for standard reads.
- Scheduler batch limits prevent bursts (e.g., 40 marketing emails/tick, 20 greeting recipients/tick).

### 12.2 Reliability
- Payment confirmation idempotent and replay-protected; publication actions idempotent.
- Scheduled jobs tolerate retries and partial failures; TTL + retry for push/email send paths.
- API errors recoverable with safe retry; mobile network-error + retry states.

### 12.3 Security & privacy
- Backend-enforced authorization; UI hiding is not a boundary.
- Ownership, school membership, class assignment, enrollment, publication validated server-side.
- bcrypt password hashing; OTPs/tokens expire (OTP 5-min 2FA, configurable script OTP, TTL access tokens); OTPs hashed before storage (script sharing).
- Rate-limit auth endpoints; internal endpoints gated by `x-internal-api-key`.
- Stripe webhook signature verified; Paystack callbacks verified server-side.
- Correct answers excluded from student responses before submission/result release.
- File uploads: mime/type + size limits (logo 2 MB, CSV 5–10 MB, topic videos 500 MB); Cloudinary sanitization with local fallback.
- Secrets only via env vars; security events logged without credentials/payment data; least privilege.
- Helmet, CORS allow-list, mongo-sanitize, express-validator, 50 MB JSON body limit.

### 12.4 Accessibility & usability
- Clear labels, contrast, touch targets, keyboard support, accessible roles.
- Dropdowns/toggles/selectors over ambiguous pills for single-value settings.
- Validation near field; input preserved on recoverable errors.
- Dark/light theme respected on web + mobile; no bright/flashy combinations; no white controls in dark mode.
- Forms touch-friendly; controlled inputs must not reset while typing (known bug class on mobile).

### 12.5 Observability
- Structured server errors; correlation/request IDs.
- Track auth, payment, publication, enrollment, notification, AI-provider, scheduler failures.
- Monitor API latency, job failures, websocket health, mobile crash reports.

---

## 13. Key user journeys

### 13.1 Teacher creates, publishes, and runs a class
1. Teacher signs in (passes verification + subscription gate).
2. Creates classroom (level, pricing, capacity, schedule, class format, public access window if public lecture/seminar).
3. Adds topics (manually or via AI syllabus import) and uploads videos.
4. Activates first topic → progression engine auto-advances.
5. Publishes and shares link (`/c/:shortCode`) → students discover/enroll/pay.
6. Launches live sessions (Zoom/Meet/whiteboard), tracks attendance, monitors progress.

### 13.2 Teacher creates an MCQ/theory assignment
1. Opens classroom assignment tab → create (manual or AI).
2. For MCQ: selects correct option via check icon; sets due date + result release.
3. Publishes → students notified → submit.
4. Grades MCQ automatically; theory manually or with AI marking; releases results.
5. Student receives result + can view score/feedback once released.

### 13.3 External marker grades scripts
1. Teacher enables script sharing on an exam/assignment and generates a single or group link.
2. Marker opens `/shared-script/:shareToken`, enters name/email.
3. Authority approves by sharing the OTP (sent to authority email).
4. Marker verifies OTP → time-limited session (view or grade).
5. Marker saves grading progress and finalizes → grades written back to the real submission.

### 13.4 Guest attends a public lecture
1. Student/guest opens public class page from catalog or link.
2. Chooses to join live or watch recording (within access window).
3. Pays via Paystack if required (guest checkout).
4. Records PublicAttendee; gets join instructions / recording link.
5. Host sees badges (Live Now / Recording / Window Closed) and attendee records.

### 13.5 Student completes a timed exam
1. Opens published exam via `/exam-center/:token` (registered or open).
2. App shows duration/window; starts attempt (in-progress persisted, timeRemaining).
3. Answers MCQ + theory (LaTeX supported); auto-submit at timeout.
4. Results released per config → email + in-app.

### 13.6 Teacher monetizes and gets paid
1. Sets pricing (per_lecture/per_topic/weekly/monthly/one_time/free).
2. Student pays via Paystack/Stripe → verified → enrolled → payment record with fee breakdown.
3. Payout amount accrues; root admin approves disbursement → payout email + reference.
4. Teacher configures bank details + payout frequency in Profile.

### 13.7 Teacher grows with marketing
1. Builds contacts (manual/CSV) and lists.
2. Creates templates and multi-step campaigns (or AI-generates them).
3. Launches → scheduler sends with holiday blackouts, dedupe, unsubscribe links.
4. Reviews send logs and job analytics.

---

## 14. Integrations

| Integration | Purpose | Notes |
|---|---|---|
| Paystack | Payments (NGN default) | Inline widget, verify, webhook; guest checkout |
| Stripe | Global payments + subscriptions + refunds | Checkout session, webhook, payment intents |
| Zoom | Live meetings | JWT-based API, meeting per classroom |
| Google (OAuth/Meet) | Google Meet + calendar | refresh_token stored on user |
| Groq / Gemini | AI generation | Switchable via Settings; JSON-only; PPTX via pptxgenjs |
| Brevo (Sendinblue) + Nodemailer (+ SendGrid lib) | Transactional + marketing email | Templated wrapper, unsubscribe |
| Cloudinary (+ local fallback) | File/video storage | Multer; type/size limits |
| Expo push | Mobile push notifications | expoPushToken |
| Socket.io | Real-time whiteboard + voice signaling | JWT-authenticated |
| Swagger | API docs | `/api-docs`, `/api-docs.json` |
| jsPDF / expo-print | Report export (web CSV/PDF, mobile PDF) | |

---

## 15. Environment & operations

Required env (backend): `PORT`, `MONGODB_URI`, `JWT_SECRET`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `PAYSTACK_SECRET_KEY`, `PAYSTACK_CURRENCY`, `BREVO_API_KEY`, `SMTP_*`, `GROQ_API_KEY`/`GEMINI_API_KEY` (+ model overrides), `CLOUDINARY_*`, `ZOOM_API_KEY/SECRET`, `GOOGLE_OAUTH_CLIENT_ID/SECRET`, `FRONTEND_URL`, `BACKEND_URL`, `INTERNAL_API_KEY`.

Frontend: `VITE_API_URL`, `VITE_PAYSTACK_PUBLIC_KEY`. Mobile: `EXPO_PUBLIC_API_URL`.

Scheduler (node-cron): class reminders (every minute), exam-result emails (every minute), marketing queue (every minute), marketing greeting jobs (every minute), topic auto-progression (midnight).

---

## 16. Release plan (current codebase = baseline)

| Milestone | Scope | State |
|---|---|---|
| M0 — Foundations | Auth, roles, verification, 2FA, settings, seed | ✅ Done |
| M1 — Core teaching | Schools, classrooms, topics + progression, topic/video management | ✅ Done |
| M2 — Assessments | Assignments (MCQ/theory, AI marking), exams (timed, open access, results) | ✅ Done |
| M3 — Monetization | Paystack + Stripe, enrollment, history, subscriptions, disbursements, refunds | ✅ Done |
| M4 — Live & collaboration | Whiteboard (full toolkit), WebRTC voice/video, Zoom/Meet, attendance, Q&A | ✅ Done |
| M5 — Growth | AI assistant (7 modes + marketing), marketing engine, public lectures/seminars | ✅ Done |
| M6 — External grading | Script sharing (view/grade sessions, group links) | ✅ Done |
| M7 — Insights | Role reports + CSV/PDF, feedback | ✅ Done |
| M8 — Mobile | Role-aware navigation, auth persistence, payments, AI, reports, push, deep links | Mostly done (gaps §8.2) |
| M9 — Parity & hardening | Close mobile gaps (2FA, subscription UI, authoring, root console), API auth tests, offline caching, mobile smoke tests | Planned |
| M10 — Scale | Coupons/invoices/self-serve refunds, attendance reporting, calendar sync, multi-language, marketplace | Roadmap |

---

## 17. Acceptance criteria (release gate)

- All roles see and can perform only permitted actions; verified end-to-end (API + UI).
- Unverified and pending-onboarding users routed correctly; 2FA enforced for root admin.
- Students never see unpublished classes, assignments, or exams; correct answers never leak pre-release.
- Payments record reference, payer, purpose, class/topic/subscription, fee breakdown, and date; enrollment only after verified payment.
- Script sessions expire; OTPs hashed; grades finalize correctly back to submissions.
- Public classes show correct Live/Recording/Closed state and gate guest access correctly.
- Light and dark themes readable on all surfaces; mobile deep links restore correct screen.
- Uploads, notifications, payment webhooks, and scheduled jobs tested in staging.
- API errors, loading states, empty states, retry flows present on web and mobile.

---

## 18. Risks & mitigations

| Risk | Impact | Mitigation |
|---|---|---|
| Payment webhook/verification failures lose enrollments | High | Idempotent confirmation, webhook retries, signature verification, monitoring |
| AI provider cost/errors | Medium | Draft-first UX, provider switch, JSON validation, error logging |
| Mobile/web behavior drift | Medium | Parity checklist per milestone; shared role/theme/API utilities |
| Cron misfires over-promote topics | Medium | Idempotent jobs, retries, manual set-next/reset |
| Subscription gate locks paying users | Medium | Global kill-switch (`subscriptionCheckingEnabled`) |
| Script-sharing OTP phishing/social engineering | Medium | Authority-only OTP emails, short lifespan, hashed OTPs, session TTL |
| Answer leakage in exams/assignments | High | Server-side field stripping before release; tests |
| Guest public-class abuse (spam attendance) | Medium | Rate limiting on public endpoints, attendee records |
| 2FA lockout (root admin) | Medium | Backup codes, temp login token |

---

## 19. Open questions

1. Should AI marking expose confidence or require human sign-off before grades are released?
2. Are per-lecture payments charged at session launch or before joining?
3. Do we need invoices/receipts per payment, or is history sufficient for v1?
4. Retention period for payments, submissions, script sessions, and Q&A data?
5. Should students self-serve un-enroll/refund requests, or remain admin-only?
6. Multi-language priority list?
7. Should mobile gain a full authoring experience or remain "AI + review" only (gap M1/M6)?

---

## 20. Glossary

| Term | Definition |
|---|---|
| Classroom | Primary learning container with curriculum, students, schedule, pricing |
| Class format | `classroom` / `public_lecture` / `public_seminar` |
| Public access window | `startsAt`/`endsAt` + duration for guest join / recording access |
| Topic progression | Automated pipeline: activate → complete → next (order or nextTopicId) |
| TopicProgress | Per-student video watch progress (watched ids, %, last watched) |
| CallSession | Record of a live Zoom/Meet session with optional per-lecture fee |
| PublicAttendee | Guest record for public lectures/seminars (live or recording access) |
| Script sharing | OTP-gated external view/grade access to exam/assignment answer scripts |
| Disbursement | Root-admin-approved payout with reference, fee breakdown, notifications |
| Subscription | Platform plan (trial/pay-as-you-go/weekly/monthly/yearly) for sellers |
| Personal Teacher | Independent tutor owning classrooms, students, payments, payouts |
| School Admin | Institutional manager, possibly across multiple schools |
